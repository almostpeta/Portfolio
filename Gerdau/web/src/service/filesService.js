import axiosAPI from "../api/axiosAPI";
// import { base64ToBlob } from "../lib/utils";

const filesURL = "files";

export const getFileFromName = async (fileId) => {
  // const { data, error } = await axiosAPI.get(`${filesURL}/getFile/${fileId}`);
  // if (error) {
  //   throw error;
  // }
  // const blob = data.items;
  // return base64ToBlob(blob);
};
